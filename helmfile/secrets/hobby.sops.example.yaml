# Copy to hobby.sops.yaml, set the token, configure .sops.yaml, then run:
# sops --encrypt --in-place helmfile/secrets/hobby.sops.yaml
#
# Use a narrowly scoped DigitalOcean token that can only manage Domain records.
digitaloceanToken: replace-me
